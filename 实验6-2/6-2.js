function skip1() {
    var video = document.getElementsByClassName("video1")[0];
    //设置时间的跳转，当前的时间设为5s
    video.currentTime = 5;
} 
function skip2() {
    var video = document.getElementsByClassName("video1")[0];
    video.currentTime = 75;
} 
function skip3() {
    var video = document.getElementsByClassName("video1")[0];
    video.currentTime = 210;
} 
function skip4() {
    var video = document.getElementsByClassName("video1")[0];
    video.currentTime = 400;
} 
function skip5() {
    var video = document.getElementsByClassName("video1")[0];
    video.currentTime = 530;
} 